# ===========================================
# HireReady - 環境變數設定
# ===========================================
# 複製此檔案為 .env.local 並填入實際值

# 應用程式設定
# -------------------------------------------
PORT=5555
NEXT_PUBLIC_APP_URL=http://localhost:5555

# Better Auth - 認證系統
# -------------------------------------------
# 應用程式基礎 URL（用於回調）
BETTER_AUTH_URL=http://localhost:5555
# 用於加密 session 的密鑰（請使用 openssl rand -base64 32 生成）
BETTER_AUTH_SECRET=your-secret-key-here

# Google OAuth
# -------------------------------------------
# 前往 https://console.cloud.google.com/apis/credentials 建立
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Google Gemini API
# -------------------------------------------
# 前往 https://aistudio.google.com/app/apikey 取得
GEMINI_API_KEY=your-gemini-api-key

# 資料庫 (PostgreSQL)
# -------------------------------------------
# Zeabur 會自動提供，本地開發可使用 Docker
DATABASE_URL=postgresql://user:password@localhost:5432/hireready

# Redis (可選 - 用於背景任務佇列)
# -------------------------------------------
# 本地開發：docker run -d -p 6379:6379 redis
# 如未設定，履歷解析將同步執行（不使用佇列）
REDIS_URL=redis://localhost:6379
